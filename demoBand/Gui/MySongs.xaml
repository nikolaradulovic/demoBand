<Page
    x:Class="demoBand.Gui.MySongs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:demoBand.Gui"
    xmlns:vm ="using:demoBand.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <vm:MenuView x:Key="menuView"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
               
                <GridView x:Name="gridList" Margin="30,0,0,0" DataContext="{Binding Source={StaticResource menuView}}"
                          ItemsSource="{Binding ListItems}">        
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid x:Name="gridSong" Width="300">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="5*"/>
                                </Grid.ColumnDefinitions>
                                <Image x:Name="imgLogo" Grid.Column="0" Source="Assets/Images/Icons/new-song-small.png" />
                                <TextBlock Grid.Column="1" FontSize="20" TextWrapping="WrapWholeWords" Text="Artist:"/>
                            </Grid>
                            
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
                
            </Grid>
        </Grid>
    </Grid>
</Page>
